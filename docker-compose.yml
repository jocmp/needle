services:
  postgres:
    image: postgres:16
    ports:
      - "25432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: needle_development
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
